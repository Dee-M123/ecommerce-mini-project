version: 2

models:
  - name: stg_client_type
    description: Reference table for client type values.

    columns:
      - name: client_type_id
        tests:
          - not_null
          - unique

      - name: type_name
        tests:
          - not_null

